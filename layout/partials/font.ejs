<style type="text/css">
    pre,
    code {
      font-family: <%if (theme.font.enable && theme.font.codes.enable){%>'<%= theme.font.codes.family%>', <%}%>monospace;
    }
    html {
      font-family: <%if (theme.font.enable && theme.font.else.enable){%>'<%= theme.font.else.family%>', <%}%>sans-serif;
    }
    body {
      font-family: <%if (theme.font.enable && theme.font.else.enable){%>'<%= theme.font.else.family%>', <%}%>sans-serif;
    }
    h1, h2, h3, h4, h5, figure {
      font-family: <%if (theme.font.enable && theme.font.else.enable){%>'<%= theme.font.else.family%>', <%}%>sans-serif;
    }
    .menu-container{
      font-family: <%if (theme.font.enable && theme.font.else.enable){%>'<%= theme.font.else.family%>', <%}%>sans-serif;
    }
  </style>
